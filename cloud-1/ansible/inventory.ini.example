# WordPress servers group
# EXAMPLE CONFIGURATION - DO NOT PUT REAL DATA HERE
[wordpress_servers]
# Replace x.x.x.x with your server's IP
my-server ansible_host=x.x.x.x ansible_user=debian

# Variables are defined in group_vars/all/vars.yml
# Secrets are encrypted in group_vars/all/vault.yml